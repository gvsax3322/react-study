import React from "react";

const Login = () => {
  return (
    <div>
      Login
      <div>
        <button>로그인 값 받기</button>
        <button>로그인 흉내</button>
      </div>
    </div>
  );
};

export default Login;
